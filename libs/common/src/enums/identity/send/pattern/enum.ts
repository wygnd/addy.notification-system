export enum IdentitySendPatternEnum {
  SEND_CONNECT = 'identity.connect.send',
  VERIFY_CONNECT = 'identity.connect.verify',
  CONFIRM_CONNECT = 'identity.connect.confirm',
  CHECK_CONNECT = 'identity.connect.check',
  EXISTS_CLIENT_PLATFORM = 'identity.client.platform.exists',
  DISCONNECT = 'identity.disconnect',
  GET_USER_CONNECTIONS = 'identity.user.connections',
}
